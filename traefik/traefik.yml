entryPoints:
    web:
        address: ":80"
        transport:
            respondingTimeouts:
                readTimeout: 300s

providers:
    file:
        directory: /etc/traefik
        watch: true

ping:
    entryPoint: traefik

log:
    level: INFO

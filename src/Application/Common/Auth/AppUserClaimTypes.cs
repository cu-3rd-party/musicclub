namespace CuMusicClub.Application.Common.Auth;

public static class AppUserClaimTypes
{
    /// <summary>
    /// Holds the domain user id (the <c>app_user.id</c> Guid) carried in access token claims.
    /// </summary>
    public const string AppUserId = "app_user_id";
}
